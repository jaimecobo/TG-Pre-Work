console.log(Math.random()*100);
console.log(Math.floor(Math.random()*100));
console.log(Math.ceil(43.8));
console.log(Number.isInteger(2017));

/*
- Create a random number with Math.random(), then multiply it by 100.
- Put Math.random() * 100 inside the parentheses of Math.floor().
- Method on the JavaScript Math object that returns the smallest integer greater than or equal to a decimal number (43.8).
- Method on the built-in Number object that checks if a number is an integer. 
*/
